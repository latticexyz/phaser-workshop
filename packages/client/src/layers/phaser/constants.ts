export enum Scenes {
  Main = "Main",
}

export enum Maps {
  Main = "Main",
}

export enum Animations {
  GolemIdle = "GolemIdle",
}
export enum Sprites {
}

export enum Assets {
  MainAtlas = "MainAtlas",
  Tileset = "Tileset",
}

export enum Direction {
  Unknown,
  Up,
  Down,
  Left,
  Right
}

export const TILE_HEIGHT = 32;
export const TILE_WIDTH = 32;
